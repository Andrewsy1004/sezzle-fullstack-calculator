# Base URL the frontend uses to reach the backend API.
# Leave unset to default to "/api" (same-origin, used by the Docker/nginx setup).
# For local `pnpm dev` against a Go server on :8080 running standalone, use:
VITE_API_BASE_URL=http://localhost:8080/api
